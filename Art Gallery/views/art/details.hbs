<main>
    <section id="details-page">
        <h1>Details</h1>
        <article class="details-card">

            <article class="details-card-text">
                <h2>Title:<br> {{gallery.title}}</h2>
                <h3>Author: {{author.username}}</h3>
                <h3>Painting technique: {{gallery.paintingTechnique}}</h3>
                <h3>Certificate of authenticity: {{gallery.certificate}}</h3>

                <!-- If there is no registered user, do not display buttons-->
                <div class="buttons">
                    <!-- Only for registered user and author of the publication -->
                    {{#if isAuthor}}
                    <a href="/art/{{gallery._id}}/edit" class="btn-edit">Edit</a>
                    <a href="/art/{{gallery._id}}/delete" class="btn-delete">Delete</a>
                    {{else}}
                    {{#if gallery.isPublicated}}
                    <!-- logged in user who has not yet shared the publication-->
                    <a href="#" class="btn-share">Share publication</a>
                    <!-- logged in user who has already shared the publication-->
                    {{else}}
                    <p class="shared-pub">You already shared this publication</p>
                    {{/if}}
                    {{/if}}
                </div>

            </article>

            <article class="details-card-image">
                <img src="{{gallery.artPicture}}" alt="art-image2">
            </article>

        </article>
    </section>
</main>